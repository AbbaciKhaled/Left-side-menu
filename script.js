function openMenu() {
    document.getElementById("menu").className != "close-menu" ? document.getElementById("menu").className = "close-menu" : document.getElementById("menu").className = "open-menu"
}